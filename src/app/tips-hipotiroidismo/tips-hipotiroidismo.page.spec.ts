import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TipsHipotiroidismoPage } from './tips-hipotiroidismo.page';

describe('TipsHipotiroidismoPage', () => {
  let component: TipsHipotiroidismoPage;
  let fixture: ComponentFixture<TipsHipotiroidismoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TipsHipotiroidismoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
